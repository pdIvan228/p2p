# Исходные значения переменных
a = 10
b = 5

# Вывод исходных значений переменных
print("До обмена:")
print("a =", a)
print("b =", b)

# Обмен значений переменных без использования третьей переменной
a = a + b  # Суммируем значения a и b и сохраняем в a
b = a - b  # Из суммы a + b вычитаем b, чтобы получить исходное значение a, сохраняем в b
a = a - b  # Из суммы a + b вычитаем новое значение b, чтобы получить исходное значение b, сохраняем в a

# Вывод значений переменных после обмена
print("После обмена:")
print("a =", a)
print("b =", b)
